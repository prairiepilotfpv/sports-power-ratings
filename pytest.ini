[pytest]
markers =
    slow: longer-running tests (deselect with -m "not slow")
filterwarnings =
    ignore:Invalid BT prediction.*margin_sd must be at least:RuntimeWarning
    ignore:The behavior of DataFrame concatenation with empty or all-NA entries is deprecated.:FutureWarning
